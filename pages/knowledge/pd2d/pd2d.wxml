<!--pages/knowledge/pd2d/pd2d.wxml-->
<view class="page">
  <!-- 导航栏 -->
  <view class="custom-nav" style="padding-top: {{statusBarHeight}}px;">
    <view class="nav-bar" style="height: {{navBarHeight}}px;">
      <view class="nav-back" bindtap="goBack">
        <text class="nav-back-icon">‹</text>
      </view>
      <text class="nav-title">PD2D</text>
      <view class="nav-mode" bindtap="switchMode" wx:if="{{hasPhoto}}">
        <text class="nav-mode-text">{{mode === 'corners' ? '摆放' : '调角'}}</text>
      </view>
    </view>
  </view>

  <!-- Canvas -->
  <view class="canvas-wrapper" style="padding-top: {{statusBarHeight + navBarHeight}}px; padding-bottom: 20rpx;">
    <canvas
      type="2d"
      id="pd2dCanvas"
      class="pd2d-canvas"
      style="width: {{canvasWidth}}px; height: {{canvasHeight}}px;"
      bindtouchstart="onCanvasTouchStart"
      bindtouchmove="onCanvasTouchMove"
      bindtouchend="onCanvasTouchEnd"
    ></canvas>
  </view>

  <!-- 底部控制栏 -->
  <view class="bottom-bar">
    <!-- 墙面尺寸 + 照片按钮行 -->
    <view class="bar-row dims-row">
      <text class="dims-label">墙面</text>
      <input class="dims-input" type="number" value="{{wallWidth}}" bindinput="onWallWidthInput" />
      <text class="dims-label">×</text>
      <input class="dims-input" type="number" value="{{wallHeight}}" bindinput="onWallHeightInput" />
      <text class="dims-label">cm</text>
      <view class="btn btn-small btn-primary" bindtap="choosePhoto">
        <text>{{hasPhoto ? '换图' : '上传'}}</text>
      </view>
      <view class="btn btn-small btn-outline" bindtap="removePhoto" wx:if="{{hasPhoto}}">
        <text>移除</text>
      </view>
    </view>

    <!-- 柜体类型行 -->
    <view class="bar-row cabinet-row" wx:if="{{hasPhoto}}">
      <view class="chip {{selectedType === 'a' ? 'chip-active' : ''}}" bindtap="selectType" data-type="a">
        <text>A型</text>
      </view>
      <view class="chip {{selectedType === 'b' ? 'chip-active' : ''}}" bindtap="selectType" data-type="b">
        <text>B型</text>
      </view>
      <view class="chip {{selectedType === 'c' ? 'chip-active' : ''}}" bindtap="selectType" data-type="c">
        <text>C型</text>
      </view>
      <view class="chip {{selectedType === 'd' ? 'chip-active' : ''}}" bindtap="selectType" data-type="d">
        <text>D型</text>
      </view>
    </view>

    <!-- 宽度 + 放置行 -->
    <view class="bar-row cabinet-row" wx:if="{{hasPhoto}}">
      <view class="chip {{selectedWidth === 50 ? 'chip-active' : ''}}" bindtap="selectWidth" data-width="50">
        <text>50cm</text>
      </view>
      <view class="chip {{selectedWidth === 100 ? 'chip-active' : ''}}" bindtap="selectWidth" data-width="100">
        <text>100cm</text>
      </view>
      <view class="btn btn-place" bindtap="switchMode">
        <text>{{mode === 'place' ? '点击照片放置 ' + selectedType.toUpperCase() + '型' + selectedWidth : '切换至摆放模式'}}</text>
      </view>
    </view>

    <!-- 工具按钮行 -->
    <view class="bar-row" wx:if="{{hasPhoto}}">
      <view class="btn btn-small btn-outline" bindtap="resetCorners" wx:if="{{mode === 'corners'}}">
        <text>重置角点</text>
      </view>
      <view class="btn btn-small btn-outline" bindtap="removeLastModule" wx:if="{{modules.length > 0}}">
        <text>撤销柜体</text>
      </view>
    </view>
  </view>

  <!-- 空状态提示 -->
  <view class="hint" wx:if="{{!hasPhoto}}">
    <text>上传墙面照片 · 设置墙面尺寸 · 摆放柜体</text>
  </view>
</view>
