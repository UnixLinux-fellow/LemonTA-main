<!--pages/knowledge/pd2d/pd2d.wxml-->
<view class="page">
  <view class="custom-nav" style="padding-top: {{statusBarHeight}}px;">
    <view class="nav-bar" style="height: {{navBarHeight}}px;">
      <view class="nav-back" bindtap="goBack">
        <text class="nav-back-icon">‹</text>
      </view>
      <text class="nav-title">PD2D</text>
    </view>
  </view>

  <view class="canvas-wrapper" style="padding-top: {{statusBarHeight + navBarHeight}}px;">
    <canvas
      type="2d"
      id="pd2dCanvas"
      class="pd2d-canvas"
      style="width: {{canvasWidth}}px; height: {{canvasHeight}}px;"
      bindtouchstart="onCanvasTouchStart"
      bindtouchmove="onCanvasTouchMove"
      bindtouchend="onCanvasTouchEnd"
    ></canvas>
  </view>

  <view class="bottom-bar">
    <view class="btn btn-primary" bindtap="choosePhoto">
      <text>{{hasPhoto ? '更换照片' : '上传照片'}}</text>
    </view>
    <view class="btn btn-outline" bindtap="removePhoto" wx:if="{{hasPhoto}}">
      <text>移除照片</text>
    </view>
    <view class="btn btn-outline" bindtap="resetCorners" wx:if="{{hasPhoto}}">
      <text>重置角点</text>
    </view>
  </view>

  <view class="hint" wx:if="{{!hasPhoto}}">
    <text>上传墙面照片，拖拽四角标记透视区域</text>
  </view>
</view>
